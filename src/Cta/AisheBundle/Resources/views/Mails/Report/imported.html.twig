{% extends '::mail.html.twig' %}

{% block subject 'A new report has been imported by ' ~ user.getUsername() %}

{% block body_html %}
    {{ parent() }}
    {% block content %}
        Beste Administrator, <br/><br/>

        <strong>{{ user.getUsername() }}</strong> has imported a new report. <br/>
        Click <a href="{{ appUrl }}{{ path('cta_aishe_report_show', {'id': reportId}) }}" style="color: #e74739; text-decoration: underline;">here</a> to show this report.

        <br/><br/>
        <em>AIFSHE</em>
    {% endblock %}
{% endblock %}